# Quick Start

1. Edit `README.md`, `_sidebar.md`.
2. Add pages in `guides/` or `api/`.
3. Host anywhere that serves static files (GitHub Pages, Netlify, S3, etc).

## Local preview options
- **No install:** run a simple server:
```bash
  python -m http.server 5500
```